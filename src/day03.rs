extern crate test;
//use std::{fs::File, io::Read};

pub fn solve() -> (i32, i32) {
    // let file = File::open("inputs/input03.txt").unwrap();
    // for c in file.bytes() {
    //     //       println!("{}", c.unwrap());
    // }

    return (0, 0);
}

#[cfg(test)]
mod tests {
    //    use super::*;

    #[test]
    fn solution() {
        // assert_eq!((8349, 2681), solve());
    }
}
